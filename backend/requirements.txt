# AutoDiag backend bağımlılıkları (sürümler sabitlendi)

# Web framework + sunucu
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Veritabanı (PostgreSQL + pgvector)
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
pgvector==0.3.6

# Ayar / şema doğrulama
pydantic==2.10.4
pydantic-settings==2.7.1

# Embedding + sparse arama
sentence-transformers==3.3.1
rank-bm25==0.2.2

# Veri / sayısal işlemler
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.6.0
matplotlib==3.10.0

# HTTP istemci (LLM çağrısı için)
httpx==0.28.1

# Embedding domain-adaptation (fine-tune; scripts/finetune_embedding.py)
datasets>=4.0.0
accelerate>=0.26.0

# Test
pytest==8.3.4
python-multipart>=0.0.9
